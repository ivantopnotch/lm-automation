To execute tests, you must first start the selenium standalone server:

java -jar selenium-standalone-3.3.1.jar

Once it's up and running, open another bash console and enter the 
following to run all tests in the specs folder:

wdio wdio.conf.js